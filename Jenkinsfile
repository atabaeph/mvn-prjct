pipeline {
	agent any
	stages {
	
		stage("build") {
			steps {
				echo "building app..........."
			}
		}
		
		stages("test") {
			steps {
				echo "testing app............"
			}
		}
		
		stage("deploy") {
			steps {
				echo "deploy app............."
			}
		}
	}
}
